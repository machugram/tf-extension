# Changelog

## 0.1.0

- First public release
- TFVC Source Control provider driven by `tf.exe` / TEE `tf`
- Check in, get latest, checkout, add, undo, delete, rename
- History, shelve/unshelve, conflict resolve
- Auto-checkout for server workspaces
- Works in Cursor and VS Code via VSIX
